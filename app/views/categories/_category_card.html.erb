<div class="mt-2">
  <%= render 'categories/category_core', category: category, period: active_period_by_category(category) %>
</div>

<div class="ml-8 mt-2">
  <%= render_categories(category.childrens) if category.childrens.any? %>
</div>